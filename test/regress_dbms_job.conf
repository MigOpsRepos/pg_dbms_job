#-----------
#  General
#-----------
# Toogle debug mode
debug=0
# Path to the pid file
pidfile=/tmp/regress_dbms_job.pid
# Time to wait between runs in seconds. Default 1 second.
naptime=1
# log file
logfile=/tmp/regress_dbms_job.log
# Asynchrounous job running at same time limit
async_limit=10

#-----------
#  Database
#-----------
host=localhost
port=5432
database=regress_dbms_job
user=regress_dbms_job_dba
passwd=regress_dbms_job_dba

